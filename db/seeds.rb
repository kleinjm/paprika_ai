# Seed data is organized into db/seeds/*.seeds.rb files, loaded by seedbank.
# `bin/rails db:seed` runs this file plus every db/seeds/*.seeds.rb.
# Run an individual group with e.g. `bin/rails db:seed:users`.
